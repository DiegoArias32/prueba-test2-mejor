# Arquitectura del Sistema de Badge de Notificaciones

## Diagrama de Componentes

```
┌────────────────────────────────────────────────────────────────────────┐
│                           MAUI APPLICATION                              │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                         AppShell.xaml                             │  │
│  │  ┌────────────────────────────────────────────────────────────┐  │  │
│  │  │  FlyoutItem: Notificaciones                 [Badge: 3]     │  │  │
│  │  │  ├─ Icon: 🔔                                                │  │  │
│  │  │  ├─ Title: "Notificaciones"                                 │  │  │
│  │  │  └─ Badge (Border)                                          │  │  │
│  │  │      ├─ Background: #DC2626 (Red)                           │  │  │
│  │  │      ├─ IsVisible: MultiBinding                             │  │  │
│  │  │      │   ├─ Title == "Notificaciones"                       │  │  │
│  │  │      │   └─ HasUnreadNotifications == true                  │  │  │
│  │  │      └─ Text: {NotificationBadgeText}                       │  │  │
│  │  └────────────────────────────────────────────────────────────┘  │  │
│  │                                                                    │  │
│  │  BindingContext: AppShellViewModel                                │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                              │                                          │
│                              │ Binding                                  │
│                              ▼                                          │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                   AppShellViewModel.cs                            │  │
│  │  ┌────────────────────────────────────────────────────────────┐  │  │
│  │  │  Properties:                                                │  │  │
│  │  │  • UnreadNotificationCount: int                             │  │  │
│  │  │  • HasUnreadNotifications: bool                             │  │  │
│  │  │  • NotificationBadgeText: string                            │  │  │
│  │  ├────────────────────────────────────────────────────────────┤  │  │
│  │  │  Methods:                                                   │  │  │
│  │  │  • UpdateNotificationCountAsync()                           │  │  │
│  │  │  • UpdateBadgeCount(int count)                              │  │  │
│  │  │  • SetupPeriodicUpdate() - Timer 5 min                      │  │  │
│  │  ├────────────────────────────────────────────────────────────┤  │  │
│  │  │  Messaging:                                                 │  │  │
│  │  │  • Subscribe to: NotificationCountChangedMessage            │  │  │
│  │  └────────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                    ▲                                  │                 │
│                    │                                  │                 │
│                    │ Message                          │ Call Service    │
│                    │                                  │                 │
│  ┌─────────────────┴────────┐           ┌────────────▼──────────────┐  │
│  │  NotificationsViewModel  │           │  INotificationApiService  │  │
│  │  ┌────────────────────┐  │           │  ┌────────────────────┐  │  │
│  │  │ Operations:        │  │           │  │ • GetUnreadCount() │  │  │
│  │  │ • MarkAsRead()     │  │           │  │ • GetNotifications │  │  │
│  │  │ • MarkAllAsRead()  │  │           │  │ • MarkAsRead()     │  │  │
│  │  │ • Delete()         │  │           │  │ • MarkAllAsRead()  │  │  │
│  │  └────────────────────┘  │           │  └────────────────────┘  │  │
│  │           │               │           │           │              │  │
│  │           │ Send Message  │           │           │ HTTP Calls   │  │
│  │           ▼               │           │           ▼              │  │
│  │  NotificationCountChanged│           │   Backend API            │  │
│  │  Message(count)          │           │   /api/notifications     │  │
│  └──────────────────────────┘           └──────────────────────────┘  │
│                                                      │                  │
│                                                      │ Fallback         │
│                                                      ▼                  │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │              NotificationStorageService                           │  │
│  │  ┌────────────────────────────────────────────────────────────┐  │  │
│  │  │  Local Storage (SecureStorage):                            │  │  │
│  │  │  • GetUnreadCountAsync()                                   │  │  │
│  │  │  • GetAllNotificationsAsync()                              │  │  │
│  │  │  • UpdateNotificationAsync()                               │  │  │
│  │  │  • SyncWithBackendAsync()                                  │  │  │
│  │  └────────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└────────────────────────────────────────────────────────────────────────┘
```

## Flujo de Datos

### 1. Inicialización de Badge

```
┌─────────────┐
│   App Start │
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│ AppShell.xaml   │
│ OnAppearing()   │
└──────┬──────────┘
       │
       ▼
┌─────────────────────────────────┐
│ AppShellViewModel               │
│ UpdateNotificationCountAsync()  │
└──────┬──────────────────────────┘
       │
       ├──────────────────┬────────────────────┐
       │                  │                    │
       ▼                  ▼                    ▼
┌──────────────┐   ┌─────────────┐   ┌───────────────┐
│ Try API      │   │ If Success  │   │ If Fail       │
│ GetUnread    │──>│ Update Badge│   │ Use Storage   │
│ Count()      │   │ Send Message│   │ Fallback      │
└──────────────┘   └─────────────┘   └───────────────┘
                            │
                            ▼
                   ┌─────────────────┐
                   │ Badge Updates   │
                   │ • Count: 3      │
                   │ • Visible: true │
                   │ • Text: "3"     │
                   └─────────────────┘
```

### 2. Usuario Marca como Leída

```
┌──────────────────┐
│ User taps        │
│ "Mark as Read"   │
└────────┬─────────┘
         │
         ▼
┌────────────────────────────┐
│ NotificationsViewModel     │
│ MarkAsReadAsync()          │
└────────┬───────────────────┘
         │
         ├─────────────────┐
         │                 │
         ▼                 ▼
┌────────────────┐  ┌──────────────────┐
│ Update API     │  │ Update Storage   │
│ Mark as Read   │  │ notification     │
└────────┬───────┘  └────────┬─────────┘
         │                   │
         └─────────┬─────────┘
                   │
                   ▼
         ┌─────────────────────┐
         │ Get New Count       │
         │ UnreadCount = 2     │
         └──────────┬──────────┘
                    │
                    ▼
         ┌─────────────────────────────┐
         │ Send Message                │
         │ NotificationCountChanged(2) │
         └──────────┬──────────────────┘
                    │
                    ▼
         ┌─────────────────────┐
         │ AppShellViewModel   │
         │ Receives Message    │
         └──────────┬──────────┘
                    │
                    ▼
         ┌─────────────────────┐
         │ UpdateBadgeCount(2) │
         │ • Count: 2          │
         │ • Text: "2"         │
         └─────────────────────┘
                    │
                    ▼
         ┌─────────────────────┐
         │ UI Updates          │
         │ Badge shows "2"     │
         └─────────────────────┘
```

### 3. Actualización Periódica (Timer)

```
┌──────────────────────┐
│ Timer Tick           │
│ Every 5 minutes      │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────────────┐
│ AppShellViewModel            │
│ Timer.Elapsed event          │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ UpdateNotificationCount()    │
└──────────┬───────────────────┘
           │
           ├──────────────┬─────────────┐
           │              │             │
           ▼              ▼             ▼
    ┌──────────┐   ┌──────────┐  ┌──────────┐
    │ Get from │   │ Compare  │  │ If changed│
    │ API/     │──>│ with     │─>│ Update   │
    │ Storage  │   │ current  │  │ Badge    │
    └──────────┘   └──────────┘  └──────────┘
                                       │
                                       ▼
                                ┌──────────────┐
                                │ Badge Updates│
                                │ Silently     │
                                └──────────────┘
```

### 4. Nueva Notificación Push (SignalR)

```
┌──────────────────┐
│ Backend sends    │
│ SignalR event    │
└────────┬─────────┘
         │
         ▼
┌────────────────────────┐
│ SignalRService         │
│ OnNotificationReceived │
└────────┬───────────────┘
         │
         ▼
┌────────────────────────┐
│ NotificationService    │
│ SaveToStorage()        │
└────────┬───────────────┘
         │
         ▼
┌────────────────────────┐
│ NotificationsViewModel │
│ (if page is active)    │
│ Refresh()              │
└────────┬───────────────┘
         │
         ▼
┌──────────────────────────────┐
│ Send Message                 │
│ NotificationCountChanged(+1) │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────┐
│ AppShellViewModel    │
│ Badge Updates        │
│ Real-time!           │
└──────────────────────┘
```

## Comunicación entre Componentes

### Messenger Pattern (WeakReferenceMessenger)

```
┌────────────────────────────────────────────────────────┐
│                  WeakReferenceMessenger                 │
│                    (Global Singleton)                   │
└────────────────────────────────────────────────────────┘
                    ▲                    │
                    │                    │
         Send       │                    │    Receive
                    │                    │
    ┌───────────────┴──┐          ┌─────▼──────────────┐
    │                  │          │                    │
    │ Notifications    │          │  AppShell          │
    │ ViewModel        │          │  ViewModel         │
    │                  │          │                    │
    │ • MarkAsRead()   │          │ • Subscribe to     │
    │ • MarkAllAsRead()│          │   Message          │
    │ • Delete()       │          │ • Update Badge     │
    │                  │          │                    │
    │ Sends:           │          │ Receives:          │
    │ NotificationCount│          │ NotificationCount  │
    │ ChangedMessage   │          │ ChangedMessage     │
    └──────────────────┘          └────────────────────┘
```

### Data Flow Layers

```
┌──────────────────────────────────────────────────────┐
│              PRESENTATION LAYER                       │
│  • AppShell.xaml (View)                              │
│  • AppShellViewModel (ViewModel)                     │
│  • NotificationsViewModel (ViewModel)                │
│  • Converters (NotificationBadgeVisibilityConverter) │
└────────────────┬─────────────────────────────────────┘
                 │
                 │ Binding & Commands
                 │
                 ▼
┌──────────────────────────────────────────────────────┐
│              BUSINESS LOGIC LAYER                     │
│  • INotificationApiService                           │
│  • NotificationStorageService                        │
│  • Messaging (NotificationCountChangedMessage)       │
└────────────────┬─────────────────────────────────────┘
                 │
                 │ HTTP Calls & Storage Operations
                 │
                 ▼
┌──────────────────────────────────────────────────────┐
│              DATA LAYER                               │
│  • Backend API (/api/notifications)                  │
│  • SecureStorage (Local SQLite)                      │
│  • HttpClient                                        │
└──────────────────────────────────────────────────────┘
```

## Estado del Badge

### State Machine

```
┌─────────────────────────────────────────────────┐
│              BADGE STATE MACHINE                 │
└─────────────────────────────────────────────────┘

   ┌─────────────────┐
   │   Initial       │
   │  (Hidden)       │
   └────────┬────────┘
            │
            │ UnreadCount > 0
            │
            ▼
   ┌─────────────────┐
   │   Visible       │
   │  Shows Count    │
   └────────┬────────┘
            │
            │ UnreadCount = 0
            │
            ▼
   ┌─────────────────┐
   │   Hidden        │
   │                 │
   └────────┬────────┘
            │
            │ New Notification
            │
            └──────────────────┐
                               │
                               ▼
                      ┌─────────────────┐
                      │   Visible       │
                      │  Updated Count  │
                      └─────────────────┘

States:
• Hidden: HasUnreadNotifications = false, Count = 0
• Visible: HasUnreadNotifications = true, Count > 0
• Updated: Count changed, trigger animation (optional)

Transitions:
• Initial → Visible: New notification arrives
• Visible → Hidden: User marks all as read
• Visible → Visible: Count changes (mark one as read)
```

## Dependencias y Inyección

```
┌────────────────────────────────────────────────────┐
│               MauiProgram.cs                        │
│            Dependency Injection Container           │
└────────────────────────────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        │               │               │
        ▼               ▼               ▼
┌──────────────┐ ┌─────────────┐ ┌──────────────┐
│  AppShell    │ │ Notification│ │ Notification │
│  (Singleton) │ │ ApiService  │ │ Storage      │
│              │ │ (Singleton) │ │ Service      │
└──────┬───────┘ └──────┬──────┘ └──────┬───────┘
       │                │               │
       │ Constructor    │               │
       │ Injection      │               │
       └────────────────┴───────────────┘
                        │
                        ▼
            ┌──────────────────────┐
            │  AppShellViewModel   │
            │  (Created manually)  │
            └──────────────────────┘
                        │
                        │ Uses
                        ▼
            ┌──────────────────────┐
            │  INotificationApi    │
            │  Service             │
            └──────────────────────┘
```

## Performance Considerations

### Memory Management

```
┌─────────────────────────────────────────────────┐
│           MEMORY MANAGEMENT                      │
└─────────────────────────────────────────────────┘

AppShellViewModel:
├─ Lifetime: Singleton (same as AppShell)
├─ Memory: ~2KB (3 properties + timer)
├─ Timer: Cleaned up on OnDisappearing()
└─ Messenger: WeakReference (no memory leak)

NotificationCountChangedMessage:
├─ Lifetime: Transient (garbage collected)
├─ Memory: ~100 bytes (1 int property)
└─ References: Weak (automatic cleanup)

Total Overhead: < 5KB
Impact: Negligible
```

### Network Optimization

```
┌─────────────────────────────────────────────────┐
│           NETWORK OPTIMIZATION                   │
└─────────────────────────────────────────────────┘

API Calls:
├─ Initial Load: 1 call (GetUnreadCountAsync)
├─ Periodic Update: 1 call every 5 min
├─ User Action: 1 call (MarkAsRead, etc.)
└─ Fallback: Use local storage if API fails

Caching:
├─ Storage: SecureStorage (local SQLite)
├─ Cache Duration: Until manual sync
└─ Invalidation: On user actions

Estimated Traffic:
├─ Initial: ~200 bytes
├─ Periodic: ~200 bytes every 5 min
└─ Total/hour: ~2.4 KB
```

## Error Handling

```
┌─────────────────────────────────────────────────┐
│              ERROR HANDLING                      │
└─────────────────────────────────────────────────┘

UpdateNotificationCountAsync():
    Try:
        ├─ Call API.GetUnreadCountAsync()
        └─ Success → Update badge
    Catch:
        ├─ Log error
        ├─ Fallback to Storage.GetUnreadCountAsync()
        └─ Update badge with local data

Resilience:
├─ Network failures: Automatic fallback
├─ API errors: Use cached data
├─ Storage errors: Default to 0
└─ Never crash the app
```

## Conclusión

Esta arquitectura proporciona:

✅ **Separación de Responsabilidades**: View, ViewModel, Service
✅ **Comunicación Desacoplada**: Messenger pattern
✅ **Resiliencia**: Fallback a almacenamiento local
✅ **Performance**: Mínimo overhead, actualizaciones eficientes
✅ **Mantenibilidad**: Código limpio y organizado
✅ **Escalabilidad**: Fácil agregar nuevas features

El sistema está diseñado siguiendo las mejores prácticas de MAUI y MVVM, garantizando una experiencia de usuario fluida y un código mantenible.
